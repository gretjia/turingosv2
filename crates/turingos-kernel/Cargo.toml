[package]
name = "turingos-kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
turingos-adapter = { path = "../turingos-adapter" }
turingos-core = { path = "../turingos-core" }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
